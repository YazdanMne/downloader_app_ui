import 'package:flutter/material.dart';

class InProgsesPage extends StatelessWidget {
  const InProgsesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('in Progses page'),
      ),
    );
  }
}
